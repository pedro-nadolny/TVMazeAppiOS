import XCTest
@testable import LayoutExtensions

final class LayoutExtensionsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LayoutExtensions().text, "Hello, World!")
    }
}
